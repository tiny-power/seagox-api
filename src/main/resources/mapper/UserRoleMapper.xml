<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seagox.lowcode.mapper.UserRoleMapper">

    <!--查询角色下用户根据角色-->
    <select id="queryUserIdByRoleId" resultType="string">
        SELECT user_id FROM user_role
        <where>
            role_id = #{roleId}
        </where>
    </select>
    
    <!--查询列表通过角色id-->
    <select id="queryByRoleId" resultType="camelKeyMap">
        SELECT
        a.company_id as "companyId",
        b.id as "userId",
        b.name
        LEFT JOIN user_role a
        LEFT JOIN sys_account b ON b.id = a.user_id
        <where>
            a.role_id = #{roleId}
        </where>
    </select>

</mapper>
