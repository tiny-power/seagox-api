<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seagox.lowcode.business.mapper.ProjectChangeOrderItemMapper">
    <select id="queryByOrderId" resultType="com.seagox.lowcode.business.entity.ProjectChangeOrderItem">
        SELECT id,
               order_id AS orderId,
               item_name AS itemName,
               unit,
               quantity,
               unit_price AS unitPrice,
               amount,
               remark,
               sort,
               created_at AS createdAt,
               updated_at AS updatedAt
        FROM project_change_order_item
        WHERE order_id = #{orderId}
        ORDER BY sort ASC, id ASC
    </select>
</mapper>
